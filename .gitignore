node_modules
dist
.DS_Store
*.log
.vite

# Local env files — never commit secrets or keys
.env
.env.local
.env.*.local

# Claude Code's local project notes (per-checkout, not for sharing)
.claude/
.env.seed
